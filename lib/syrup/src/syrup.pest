true = @{ 't' }
false = @{ 'f' }
boolean = @{ true | false }

int = @{ ASCII_DIGIT+ ~ ('+' | '-') }

bytes = @{ ASCII_DIGIT+ ~ ':' ~ ANY+ }

string = @{ ASCII_DIGIT+ ~ '"' ~ ANY+ }

symbol = @{ ASCII_DIGIT+ ~ ''}

record = { "<" ~ ">" }
